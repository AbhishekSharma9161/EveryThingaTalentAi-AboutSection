const GradientText = ({text}) => {
  return (
    <span className="bg-gradient-to-r from-[#ec0bc8] via-[#5bb2f6] to-[#f6c436] text-transparent bg-clip-text">
      {text}
    </span>
  )
}

export default GradientText
